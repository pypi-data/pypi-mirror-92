#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['s3insync', 's3insync.cmd']

package_data = \
{'': ['*']}

install_requires = \
['boto3 ~=1.16', 'prometheus_client']

extras_require = \
{'test': ['pytest', 'moto', 'pyfakefs']}

entry_points = \
{'console_scripts': ['s3insync = s3insync.cli:main']}

setup(name='s3insync',
      version='0.2.1',
      description='Synchronise an S3 directory to the local filesystem, as a daemon',
      author='Jonathan Stott',
      author_email='jonathan.stott@gmail.com',
      url='https://github.com/namelessjon/s3insync',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
