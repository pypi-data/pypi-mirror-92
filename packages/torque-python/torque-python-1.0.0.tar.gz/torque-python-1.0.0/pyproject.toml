[tool.poetry]
name = "torque-python"
version = "1.0.0"
description = ""
authors = ["Torque, Inc. <development@torque.cloud>"]
license = "MIT"
packages = [
    { include = "torque" }
]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
