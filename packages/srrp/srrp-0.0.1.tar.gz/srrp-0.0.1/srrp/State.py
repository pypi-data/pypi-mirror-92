class State:
    def __init__(self, rho, vx, vt, pressure):
        self.rho = rho
        self.vx = vx
        self.vt = vt
        self.pressure = pressure 

    def speed():
        return np.sqrt(self.vx**2 + self.vy ** 2)

    def 