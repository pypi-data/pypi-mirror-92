from setuptools import setup

setup(name='CC_distributions',
      version='0.1',
      description='Gaussian * Binomial distributions',
      packages=['CC_distributions'],
      author='Cooper Chastain',
      zip_safe=False)
