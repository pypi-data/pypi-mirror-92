{
    "type": "object",
    "required": [
        "service_uid"
    ],
    "additionalProperties": false,
    "properties": {
        "url": {
            "type": "string",
            "title": "URI for service publishing.",
            "description": "URI to publish. Default: aoscloud.io.",
            "maxLength": 255
        },
        "service_uid": {
            "type": "string",
            "title": "Service UID (provided by Aos cloud).",
            "description": "Service UID (provided by Aos cloud).",
            "maxLength": 255
        },
        "tls_key": {
            "type": "string",
            "title": "Path to the key file for TLS connection. If absent - sign_key is used.",
            "description": "Path to the key file for TLS connection. If absent - sign_key is used.",
            "maxLength": 255
        },
        "tls_certificate": {
            "type": "string",
            "title": "Path to the certificate file for TLS connection. If absent - sign_certificate is used",
            "description": "Path to the certificate file for TLS connection. If absent - sign_certificate is used",
            "maxLength": 255
        },
        "version": {
            "type": "string",
            "title": "Service Version",
            "description": "User-defined service version",
            "maxLength": 255
        }
    }
}
