[target]
description = "XIP Kernel image"
dependencies = config
preconditions = grep -q CONFIG_XIP_KERNEL=y {build_dir}/.config
commands = {make} && {make} xipImage && find {build_dir}/arch -name xipImage -exec cp '{{}}' {build_dir}/xipImage ';'

[artifacts]
xipImage = xipImage
