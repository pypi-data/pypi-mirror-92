server {
       listen PORT;

       server_name SERVERNAME;

       location /static {
               alias /root/sources/SITENAME/static;
       }

       location / {
               proxy_set_header Host $host;
               proxy_pass http://unix:/tmp/SITENAME.socket;
       }
}
